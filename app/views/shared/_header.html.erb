<div class="jumbotron">
  <h1>CosMade <small>The best handmade costume dealer on the interwebs!</small></h1>
  Welcome, <%= @user %>
  <% if @user != nil %>
    <%= link_to "Log Out", logout_path, method: :delete, class: 'btn btn-danger' %>
  <% else %>
    <%= link_to "Log In", login_path, class: 'btn btn-default' %>
  <% end %>
</div>
